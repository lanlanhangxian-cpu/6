name: Build K10x ReSukiSU+SUSFS Kernel (5.4.233-qgki)

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 从模块仓库补全 defconfig 和设备树
      - name: 获取 defconfig 与设备树
        run: |
          git clone -b oppo/sm6375_u_14.0.0_k10x_5g --depth=1 \
            https://github.com/oppo-source/android_kernel_modules_and_devicetree_oppo_sm6375 tmp_repo
          mkdir -p arch/arm64/configs
          cp -r tmp_repo/arch/arm64/configs/* arch/arm64/configs/ 2>/dev/null || true
          if [ -d tmp_repo/arch/arm64/boot/dts ]; then
            mkdir -p arch/arm64/boot/dts
            cp -r tmp_repo/arch/arm64/boot/dts/* arch/arm64/boot/dts/
          fi
          rm -rf tmp_repo

      - name: 安装依赖
        run: sudo apt-get update && sudo apt-get install -y gcc-aarch64-linux-gnu libncurses-dev flex bison bc

      - name: 编译内核
        run: |
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          DEFCONFIG=$(find arch/arm64/configs/ -name "*_defconfig" -type f | head -1)
          make $(basename "$DEFCONFIG")
          ./scripts/config --set-str CONFIG_LOCALVERSION "-qgki-gfe724a477a80"
          make olddefconfig
          make -j$(nproc)

      - name: 打包
        run: |
          git clone https://github.com/osm0sis/AnyKernel3
          cp arch/arm64/boot/Image.gz-dtb AnyKernel3/
          if [ -d "official_modules" ]; then cp -r official_modules/* AnyKernel3/modules/; fi
          cd AnyKernel3
          sed -i 's/device.name1=.*/device.name1=oscar/' anykernel.sh
          zip -r9 ../K10x_ReSukiSU_SUSFS-v2.0.0-$(date +%Y%m%d-%H%M).zip ./*

      - name: 上传
        uses: actions/upload-artifact@v4
        with:
          name: K10x_Kernel
          path: K10x_ReSukiSU_SUSFS-v2.0.0-*.zip
